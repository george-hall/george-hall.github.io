<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://george-hall.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://george-hall.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-02-05T10:05:17+00:00</updated><id>https://george-hall.github.io/feed.xml</id><title type="html">blank</title><entry><title type="html">Summary of VEGA</title><link href="https://george-hall.github.io/blog/2026/vega/" rel="alternate" type="text/html" title="Summary of VEGA"/><published>2026-02-04T00:00:00+00:00</published><updated>2026-02-04T00:00:00+00:00</updated><id>https://george-hall.github.io/blog/2026/vega</id><content type="html" xml:base="https://george-hall.github.io/blog/2026/vega/"><![CDATA[<p><strong>DRAFT POST</strong></p> <p>This post is a rough summary of the VEGA architecture presented in <a href="https://www.nature.com/articles/s41467-021-26017-0">VEGA is an interpretable generative model for inferring biological network activity in single-cell transcriptomics</a>, published by Seninge <em>et al.</em> in <em>Nature Communications</em>. I’m hoping that writing it up here will allow me to understand it better. If you understand this work and its position in the wider context of variational autoencoders better than me and notice mistakes in my write-up, then please do get in touch via the link at the bottom of this page!</p> <p><br/></p> <p>VEGA (“VAE Enhanced by Gene Annotations”) is a variational autoencoder (VAE) built to reduce dimensionality of a single-cell transcriptomic dataset by more meaningfully capture the biological information driving variance within it. This means that, in order to understand what VEGA is, we must first understand VAEs. The first part of this post will deal with that and then we will revisit VEGA.</p> <p><br/></p> <h3 id="variational-autoencoders-vaes">Variational autoencoders (VAEs)</h3> <p><strong>This section is largely based on / inspired by a nice <a href="https://medium.com/h7w/an-introduction-to-variational-autoencoders-vaes-305bacb75fbb">post</a> by Francesco Franco</strong></p> <p>An autoencoder (AE) is a pair of connected neural networks (an <em>encoder</em> and a <em>decoder</em>) that aims to find a lower-dimensionality representation of a dataset. As you might expect, the encoder encodes data points and the decoder decodes them again. Graphically:</p> <p><img src="/assets/img/autoencoder_diagram.png" alt="image" style="height: auto; width: 80%; margin:auto; display: block"/></p> <p>(yes, there is a node connected to nothing in the second hidden layer 🤦 - this is just a diagram bug…) The bottleneck layer (labelled “latent” above) is the reduced dimension representation of the instance used as input on the left of the diagram. The decoder portion of the model takes the latent representation and attempts to recreate the original instance. If a dataset is able to pass through the autoencoder with minimal reconstruction loss (i.e. artefacts introduced by the compression), then we know that the latent representation in the middle, when coupled with the trained decoder network, is a reasonable compressed representation of the data.</p> <p><br/></p> <p>A decoder can be used to generate novel instances following the distribution of the training data by ‘decoding’ a point in the latent space (i.e. assigning arbitrary values latent layer). However, it turns out that a standard autoencoder does a bad job of generating realistic data: although the latent space surrounding training examples yield realistic novel data, the space far from training data does not. Ideally, we would develop an autoencoder that learns a latent representation that generates realistic data everywhere.</p> <p>Variational autoencoders (VAEs) are able to achieve this aim by smoothing the latent space between the training points. Instead of learning single representations for each instance, a VAE instead learns a probability distribution over the latent space. The latent nodes correspond to the parameters of this distribution. Thus, in a VAE, the input to the decoder is a sample from this learned probability distribution. Using probability distributions smooths the areas around the training instances, leading to better generation.</p> <p>[I need to detail this much better – e.g. <em>why</em> does a VAE yield smoother latent space?]</p> <p><br/></p> <h3 id="vega">VEGA</h3> <p>We are now in a position to better understand VEGA. Instead of learning a latent representation without biological meaning, VEGA attempts to learn a latent representation of a single-cell transcriptomic dataset that captures the expression of gene sets by each cell. It associates each latent node of the VAE with a gene set and uses a decoder with a single, linear layer in which an output node (i.e. a gene) is only connected to the latent nodes representing gene sets to which it belongs. In this way, cells that have similar latent representations in VEGA likely express similar gene sets. The following a schematic for VEGA:</p> <p><img src="/assets/img/vega_diagram.png" alt="image" style="height: auto; width: 80%; margin:auto; display: block"/></p> <p>The input and output nodes correspond to genes G1-G6 and the latent nodes correspond to gene sets GS1-GS3.</p> <p><br/></p> <h4 id="any-comments">Any comments?</h4> <p>Feel free to submit an issue <a href="https://github.com/george-hall/george-hall.github.io/issues">here</a>.</p> <p><br/></p> <hr/> <h6><small> <b>Disclaimer:</b> The content in this post is provided as is, without warranty of any kind. I make no guarantees about the completeness, reliability, or accuracy of the code, tips, or advice presented. Any action you take based on this content is strictly at your own risk. I will not be liable for any losses, damages, or issues arising from the use or misuse of this information, including (but not limited to) loss of data, system failures, or security vulnerabilities. Always test code and approaches in a safe environment before deploying them in production. </small></h6>]]></content><author><name></name></author><category term="single-cell"/><category term="machine-learning"/><category term="autoencoders"/><summary type="html"><![CDATA[DRAFT POST -- A summary of the autoencoder architecture of VEGA ("VAE Enhanced by Gene Annotations").]]></summary></entry><entry><title type="html">Git basics</title><link href="https://george-hall.github.io/blog/2026/into-to-git/" rel="alternate" type="text/html" title="Git basics"/><published>2026-01-30T00:00:00+00:00</published><updated>2026-01-30T00:00:00+00:00</updated><id>https://george-hall.github.io/blog/2026/into-to-git</id><content type="html" xml:base="https://george-hall.github.io/blog/2026/into-to-git/"><![CDATA[<p>Git is a tool for <em>version control</em>, a way to track changes to your files. If used correctly, you never need worry about modifying your code as all previous versions will still be accessible.</p> <p><br/></p> <h3 id="getting-started">Getting started</h3> <p>Let’s get started. For a project where you want to track changes to files, you first create a git <em>repository</em>. In the top-level directory of the project, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git init
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## Initialized empty Git repository in /private/tmp/git_tutorial/.git/
</code></pre></div></div> <p>This creates a directory called <code class="language-plaintext highlighter-rouge">.git</code> (hidden by default as its name start with a dot). We don’t need to worry about the contents of this directory in this post. We can set the username and email address used in this repository with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config user.name <span class="s2">"Example User"</span>
git config user.email <span class="s2">"example@example.com"</span>
</code></pre></div></div> <p>Let’s create a file called <code class="language-plaintext highlighter-rouge">file.txt</code> and store the text <code class="language-plaintext highlighter-rouge">This is file.txt</code> inside it.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s2">"This is file.txt."</span> <span class="o">&gt;</span> file.txt
<span class="nb">head </span>file.txt
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## This is file.txt.
</code></pre></div></div> <p><br/></p> <h3 id="your-first-commit">Your first commit</h3> <p>If we want to add <code class="language-plaintext highlighter-rouge">file.txt</code> to our tracking system, we first need to <em>commit</em> it to the repository. When a file is committed, its contents at that point in time are stored in the repository. Committing a file is a two step process. First, we <em>stage</em> the file to be committed. We then perform the commit. The staging process allows us to mark multiple files to be committed at the same time.</p> <p>If we want to see the status of files in our repository, we can run <code class="language-plaintext highlighter-rouge">git status</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## On branch main
## 
## No commits yet
## 
## Untracked files:
##   (use "git add &lt;file&gt;..." to include in what will be committed)
## 	file.txt
## 
## nothing added to commit but untracked files present (use "git add" to track)
</code></pre></div></div> <p>This tells us that we haven’t yet made any commits and that the directory contains one untracked (i.e. uncommitted) file. Let’s now stage this file to be committed and check <code class="language-plaintext highlighter-rouge">git status</code> again:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add file.txt
git status
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## On branch main
## 
## No commits yet
## 
## Changes to be committed:
##   (use "git rm --cached &lt;file&gt;..." to unstage)
## 	new file:   file.txt
</code></pre></div></div> <p>Now it tells us that <code class="language-plaintext highlighter-rouge">file.txt</code> is a new file that is staged for the next commit. Let’s now do this commit and add a message so that we know what we have changed:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">-m</span> <span class="s2">"Initial commit of file.txt."</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## [main (root-commit) 767a2ea] Initial commit of file.txt.
##  1 file changed, 1 insertion(+)
##  create mode 100644 file.txt
</code></pre></div></div> <p>We have now made our first commit! We can see it in the log:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## commit 767a2ea758c4a36e7fa626dd06ffb31804b6b8a0
## Author: Example User &lt;example@example.com&gt;
## Date:   Mon Feb 2 19:10:54 2026 +0000
## 
##     Initial commit of file.txt.
</code></pre></div></div> <p><br/></p> <h3 id="making-more-commits">Making more commits</h3> <p>Let’s change the contents of <code class="language-plaintext highlighter-rouge">file.txt</code> by adding another line of text at the end:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s2">"Another line."</span> <span class="o">&gt;&gt;</span> file.txt
</code></pre></div></div> <p>We can see all changes since the last commit with <code class="language-plaintext highlighter-rouge">git diff</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## diff --git a/file.txt b/file.txt
## index e62236e..5dc90f6 100644
## --- a/file.txt
## +++ b/file.txt
## @@ -1 +1,2 @@
##  This is file.txt.
## +Another line.
</code></pre></div></div> <p>The final line of this output shows the new line that has been added. Running <code class="language-plaintext highlighter-rouge">git status</code> shows that a tracked (i.e. previously committed) file has been modified but not yet staged for commit:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## On branch main
## Changes not staged for commit:
##   (use "git add &lt;file&gt;..." to update what will be committed)
##   (use "git restore &lt;file&gt;..." to discard changes in working directory)
## 	modified:   file.txt
## 
## no changes added to commit (use "git add" and/or "git commit -a")
</code></pre></div></div> <p>Let’s now stage it, make the commit, and check the log:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add file.txt
git commit <span class="nt">-m</span> <span class="s2">"Added a new line."</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## [main c7f1d0a] Added a new line.
##  1 file changed, 1 insertion(+)
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## commit c7f1d0a5fbe2e5ab6973139ff484862a181e7e9b
## Author: Example User &lt;example@example.com&gt;
## Date:   Mon Feb 2 19:10:54 2026 +0000
## 
##     Added a new line.
## 
## commit 767a2ea758c4a36e7fa626dd06ffb31804b6b8a0
## Author: Example User &lt;example@example.com&gt;
## Date:   Mon Feb 2 19:10:54 2026 +0000
## 
##     Initial commit of file.txt.
</code></pre></div></div> <p>Finally, let’s create a new file, <code class="language-plaintext highlighter-rouge">new_file.txt</code>, with some text in it and also modify <code class="language-plaintext highlighter-rouge">file.txt</code> again.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s2">"This a new file."</span> <span class="o">&gt;</span> new_file.txt
<span class="nb">echo</span> <span class="s2">"A third line!"</span> <span class="o">&gt;&gt;</span> file.txt
</code></pre></div></div> <p>Now, <code class="language-plaintext highlighter-rouge">git status</code> tells us that we have created a new, untracked file (<code class="language-plaintext highlighter-rouge">new_file.txt</code>) and also that we have modified our tracked file (<code class="language-plaintext highlighter-rouge">file.txt</code>):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## On branch main
## Changes not staged for commit:
##   (use "git add &lt;file&gt;..." to update what will be committed)
##   (use "git restore &lt;file&gt;..." to discard changes in working directory)
## 	modified:   file.txt
## 
## Untracked files:
##   (use "git add &lt;file&gt;..." to include in what will be committed)
## 	new_file.txt
## 
## no changes added to commit (use "git add" and/or "git commit -a")
</code></pre></div></div> <p>We can stage both files to be included in the same commit. Let’s do that:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add file.txt new_file.txt
git commit <span class="nt">-m</span> <span class="s2">"Some new changes."</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## [main 2fa28d2] Some new changes.
##  2 files changed, 2 insertions(+)
##  create mode 100644 new_file.txt
</code></pre></div></div> <p><br/></p> <h3 id="you-now-know-git">You now know git</h3> <p>At a basic level, you now know all you need to about git: you can track files and, when you want to save a snapshot of the directory, you can stage and commit them. In this way, as long as you don’t damage or delete the <code class="language-plaintext highlighter-rouge">.git</code> directory, you are safe to modify your files as you wish as you can always revert back to an earlier version!</p> <p>Ideally, each commit of your code should relate to a specific change. However, it can be hard to get a feel for how large a change should be to be committed. To start getting in the habit of version control, I recommend simply committing your code at the end of each day. This is far from best practice, but will at least mean that you have regular snapshots of your project, which is better than nothing! Once you are comfortable doing this, you can start trying to align your commits to specific changes.</p> <p>Git is a very complex tool, designed to handle large software engineering projects with many collaborators. As such, I have omitted nearly all details in an effort to provide an gentle introduction. There are many good resources where you can learn more. GitHub has <a href="docs.github.com">nice documentation</a> for both git itself and for its hosting service. For a free, in-depth guide to the details of git, I recommend <a href="https://git-scm.com/book/en/v2">Pro Git</a>. This starts from basics and quickly builds in complexity.</p> <p><br/></p> <h3 id="remote-repositories-github">Remote repositories (GitHub)</h3> <p>You may be interested on hosting your code on an external site, e.g. <a href="https://github.com/">GitHub</a>. Note that these hosting services are independent of git: git is software to track changes whilst hosting sites provide nice, graphical means of interacting with your repository and collaborating with others.</p> <p>To host a copy of your repository on GitHub, I’ll refer you to their <a href="https://docs.github.com/en/get-started/start-your-journey">documentation</a> to avoid duplicating it here. Briefly, though, you need to create a GitHub account, authenticate yourself on your local computer, and finally tell git where the GitHub copy of the repository is located.</p> <p>The authentication process can seem complicated at first, but the following pages provide a useful guide. First, you must <a href="https://docs.github.com/en/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent">generate a new ssh key</a> - this is like a password for two computer to communicate. Second, you must <a href="https://docs.github.com/en/authentication/connecting-to-github-with-ssh/adding-a-new-ssh-key-to-your-github-account">associate this key with your account</a>. Once you have completed these two steps, you can create a new repository on GitHub and then <em>push</em> your local repository there following the instructions it will display.</p> <p><br/></p> <h4 id="any-comments">Any comments?</h4> <p>Feel free to submit an issue <a href="https://github.com/george-hall/george-hall.github.io/issues">here</a>.</p> <p><br/></p> <hr/> <h6><small> <b>Disclaimer:</b> The content in this post is provided as is, without warranty of any kind. I make no guarantees about the completeness, reliability, or accuracy of the code, tips, or advice presented. Any action you take based on this content is strictly at your own risk. I will not be liable for any losses, damages, or issues arising from the use or misuse of this information, including (but not limited to) loss of data, system failures, or security vulnerabilities. Always test code and approaches in a safe environment before deploying them in production. </small></h6>]]></content><author><name></name></author><category term="git"/><summary type="html"><![CDATA[An introduction to version control with git with an emphasis on getting you comfortable using it without unnecessary details.]]></summary></entry><entry><title type="html">Running Ray Tune on Sun Grid Engine</title><link href="https://george-hall.github.io/blog/2026/ray-tune-sge/" rel="alternate" type="text/html" title="Running Ray Tune on Sun Grid Engine"/><published>2026-01-05T00:00:00+00:00</published><updated>2026-01-05T00:00:00+00:00</updated><id>https://george-hall.github.io/blog/2026/ray-tune-sge</id><content type="html" xml:base="https://george-hall.github.io/blog/2026/ray-tune-sge/"><![CDATA[<p><a href="https://www.ray.io/">Ray</a> is a framework to distribute machine learning processes in parallel and thus accelerate them. <a href="https://docs.ray.io/en/latest/tune/index.html">Ray Tune</a> is a component of Ray that allows automated hyperparameter optimization. Whilst Ray can <a href="https://docs.ray.io/en/latest/cluster/vms/user-guides/community/index.html">run natively</a> on a number of cluster managers, it does not have native support for the common Sun Grid Engine (SGE) cluster software. In this post, we show a straightforward way to run it on this engine. It does not allow the Ray scheduler the same level of control over which jobs are run and when, but it is better than nothing!</p> <p>In this post, I assume that the reader has a basic understanding of how to use Ray Tune. Very briefly, it generates new hyperparameter configurations using a method <a href="https://docs.ray.io/en/latest/tune/api/suggestion.html">specified by the user</a> (e.g. <a href="https://github.com/optuna/optuna">Optuna</a> or <a href="https://arxiv.org/abs/1801.01596">BOHB</a>) and uses a function <code class="language-plaintext highlighter-rouge">evaluate</code> to assess their relative qualities and thus guide the search (plug: how best to do this is the topic of my <a href="https://etheses.whiterose.ac.uk/id/eprint/29537/">doctoral research</a>).</p> <p>We can allow Ray Tune to run on SGE by modifying the <code class="language-plaintext highlighter-rouge">evaluate</code> function to submit each job to the cluster using the standard SGE <code class="language-plaintext highlighter-rouge">qsub</code> command. The <code class="language-plaintext highlighter-rouge">evaluate</code> function below takes a hyperparameter configuration <code class="language-plaintext highlighter-rouge">config</code> and additional arguments required to run the job on the cluster (conda environment, resource requests):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">subprocess</span>
<span class="kn">from</span> <span class="n">ray</span> <span class="kn">import</span> <span class="n">tune</span>

<span class="k">def</span> <span class="nf">evaluate</span><span class="p">(</span>
    <span class="n">config</span><span class="p">,</span>
    <span class="n">conda_init_path</span><span class="p">,</span>
    <span class="n">conda_env_name</span><span class="p">,</span>
    <span class="n">work_dir</span><span class="p">,</span>
    <span class="n">qsub_h_rt</span><span class="p">,</span>
    <span class="n">qsub_mem</span><span class="p">,</span>
    <span class="n">qsub_smp</span><span class="p">,</span>
    <span class="n">qsub_gpu</span><span class="p">,</span>
<span class="p">):</span>
    <span class="n">trial_name</span> <span class="o">=</span> <span class="n">tune</span><span class="p">.</span><span class="nf">get_context</span><span class="p">().</span><span class="nf">get_trial_name</span><span class="p">()</span>
    <span class="c1"># Blank if no GPU requested, otherwise a request for qsub_gpu many:
</span>    <span class="n">gpu_str</span> <span class="o">=</span> <span class="sh">""</span> <span class="k">if</span> <span class="n">qsub_gpu</span> <span class="o">==</span> <span class="mi">0</span> <span class="k">else</span> <span class="sa">f</span><span class="sh">"</span><span class="s">#$ -l gpu=</span><span class="si">{</span><span class="n">qsub_gpu</span><span class="si">}</span><span class="sh">"</span>
    <span class="n">script</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"""</span><span class="s">
#!/bin/bash -l

#$ -l h_rt=</span><span class="si">{</span><span class="n">qsub_h_rt</span><span class="si">}</span><span class="s">

#$ -l mem=</span><span class="si">{</span><span class="n">qsub_mem</span><span class="si">}</span><span class="s">
#$ -pe smp </span><span class="si">{</span><span class="n">qsub_smp</span><span class="si">}</span><span class="s">
</span><span class="si">{</span><span class="n">gpu_str</span><span class="si">}</span><span class="s">

# Set the name of the job.
#$ -N </span><span class="si">{</span><span class="n">trial_name</span><span class="si">}</span><span class="s">

#$ -o job_stdout_</span><span class="si">{</span><span class="n">trial_name</span><span class="si">}</span><span class="s">.txt
#$ -e job_stderr_</span><span class="si">{</span><span class="n">trial_name</span><span class="si">}</span><span class="s">.txt

#$ -wd </span><span class="si">{</span><span class="n">work_dir</span><span class="si">}</span><span class="s">

source </span><span class="si">{</span><span class="n">conda_init_path</span><span class="si">}</span><span class="s">
conda activate </span><span class="si">{</span><span class="n">conda_env_name</span><span class="si">}</span><span class="s">

# Run target `algorithm` with hyperparameter configuration `config`
algorithm --arg1 config.arg1 --arg2 config.arg2
</span><span class="sh">"""</span>

    <span class="n">job_script_path</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">config</span><span class="p">.</span><span class="n">output_path</span><span class="p">,</span> <span class="sh">"</span><span class="s">job_script.sh</span><span class="sh">"</span><span class="p">)</span>

    <span class="k">with</span> <span class="nf">open</span><span class="p">(</span><span class="n">job_script_path</span><span class="p">,</span> <span class="sh">"</span><span class="s">w</span><span class="sh">"</span><span class="p">,</span> <span class="n">encoding</span><span class="o">=</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
        <span class="n">f</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="n">script</span><span class="p">)</span>

    <span class="c1"># Submit to cluster using `qsub`
</span>    <span class="c1"># The `-sync` argument makes the `subprocess.call` process below hang until
</span>    <span class="c1"># the submitted job has finished.
</span>    <span class="n">subprocess</span><span class="p">.</span><span class="nf">call</span><span class="p">([</span><span class="sh">"</span><span class="s">qsub</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">-sync</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">y</span><span class="sh">"</span><span class="p">,</span> <span class="n">job_script_path</span><span class="p">])</span>

    <span class="c1"># Compute config quality once job has finished
</span>    <span class="n">config_quality</span> <span class="o">=</span> <span class="nf">get_config_quality</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">config_quality</span>
</code></pre></div></div> <p>In this way, the SGE scheduler takes care of the queue and resource requests, allowing the user to run their Ray Tune jobs on an SGE-based cluster</p> <p><br/></p> <h4 id="any-comments">Any comments?</h4> <p>Feel free to submit an issue <a href="https://github.com/george-hall/george-hall.github.io/issues">here</a>.</p> <p><br/></p> <hr/> <h6><small> <b>Disclaimer:</b> The content in this post is provided as is, without warranty of any kind. I make no guarantees about the completeness, reliability, or accuracy of the code, tips, or advice presented. Any action you take based on this content is strictly at your own risk. I will not be liable for any losses, damages, or issues arising from the use or misuse of this information, including (but not limited to) loss of data, system failures, or security vulnerabilities. Always test code and approaches in a safe environment before deploying them in production. </small></h6>]]></content><author><name></name></author><category term="unix"/><category term="ray-tune"/><category term="machine-learning"/><category term="hyperparameter-optimization"/><summary type="html"><![CDATA[The Ray Tune hyperparameter optimization framework does not natively support the Sun Grid Engine (SGE) job submission system. This post shows a way to implement support.]]></summary></entry><entry><title type="html">My configuration files</title><link href="https://george-hall.github.io/blog/2025/command-line-configuration-files/" rel="alternate" type="text/html" title="My configuration files"/><published>2025-12-18T00:00:00+00:00</published><updated>2025-12-18T00:00:00+00:00</updated><id>https://george-hall.github.io/blog/2025/command-line-configuration-files</id><content type="html" xml:base="https://george-hall.github.io/blog/2025/command-line-configuration-files/"><![CDATA[<p>I have been using the command line nearly every day for over a decade now. At first, this way of working feels inefficient, but you soon learn small changes that can be made to the interface to make your life easier. This page contains the most useful settings that I have collected over the years. If you see anything you think will be useful, you can copy the lines into your own configuration files. They are stored in your home directory and are hidden since their names begin with a dot.</p> <p>The <code class="language-plaintext highlighter-rouge">.bashrc</code> and <code class="language-plaintext highlighter-rouge">.inputrc</code> files control the behaviour of <code class="language-plaintext highlighter-rouge">bash</code> and the way in which the user can enter text at the command line, respectively. The <code class="language-plaintext highlighter-rouge">.vimrc</code> file is a little different since it controls a specific program: my favourite text editor, <code class="language-plaintext highlighter-rouge">vim</code>, so you only need to look at those options if you use that software.</p> <p><br/></p> <h4 id="bashrc"><code class="language-plaintext highlighter-rouge">.bashrc</code></h4> <p>This file is loaded (“sourced”) automatically at the start of a new <code class="language-plaintext highlighter-rouge">bash</code> session. It can be loaded in a running session with <code class="language-plaintext highlighter-rouge">source ~/.bashrc</code>. My file makes extensive use of aliases, which allow commands to be given other names. For example, the first alias means that if the user types <code class="language-plaintext highlighter-rouge">sl</code> then <code class="language-plaintext highlighter-rouge">bash</code> will behave as if they have typed <code class="language-plaintext highlighter-rouge">ls</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Stop `ls` typo</span>
<span class="nb">alias </span><span class="nv">sl</span><span class="o">=</span><span class="s1">'ls'</span>

<span class="c"># Make `ls` print more details, sorted in reverse chronological order with</span>
<span class="c"># human-readable file sizes.</span>
<span class="nb">alias </span><span class="nv">lsa</span><span class="o">=</span><span class="s1">'ls -lrthG'</span>
<span class="c"># Map all permuations of `lsa` back to `lsa`</span>
<span class="nb">alias </span><span class="nv">sla</span><span class="o">=</span><span class="s1">'lsa'</span>
<span class="nb">alias </span><span class="nv">sal</span><span class="o">=</span><span class="s1">'lsa'</span>
<span class="nb">alias </span><span class="nv">als</span><span class="o">=</span><span class="s1">'lsa'</span>
<span class="nb">alias </span><span class="nv">asl</span><span class="o">=</span><span class="s1">'lsa'</span>
<span class="nb">alias </span><span class="nv">las</span><span class="o">=</span><span class="s1">'lsa'</span>
<span class="c"># Another typo prevention</span>
<span class="nb">alias </span><span class="nv">as</span><span class="o">=</span><span class="s1">'lsa'</span>

<span class="c"># Move up one/two/etc directory with ../.../etc</span>
<span class="nb">alias</span> ..<span class="o">=</span><span class="s1">'cd ..'</span>
<span class="nb">alias</span> ...<span class="o">=</span><span class="s1">'cd ../../'</span>
<span class="nb">alias</span> ....<span class="o">=</span><span class="s1">'cd ../../../'</span>
<span class="nb">alias</span> .....<span class="o">=</span><span class="s1">'cd ../../../../'</span>

<span class="c"># Add colour to grep</span>
<span class="nb">alias grep</span><span class="o">=</span><span class="s1">'grep --color=auto'</span>

<span class="c"># Shortcut for `git status`</span>
<span class="nb">alias </span><span class="nv">st</span><span class="o">=</span><span class="s1">'git status'</span>

<span class="c"># More typos</span>
<span class="nb">alias </span><span class="nv">les</span><span class="o">=</span><span class="s1">'less'</span>
<span class="nb">alias </span><span class="nv">kess</span><span class="o">=</span><span class="s1">'less'</span>

<span class="c"># Keep much more history</span>
<span class="nb">export </span><span class="nv">HISTCONTROL</span><span class="o">=</span>ignoredups:erasedups
<span class="nb">export </span><span class="nv">HISTFILESIZE</span><span class="o">=</span>500000
<span class="nb">export </span><span class="nv">HISTSIZE</span><span class="o">=</span>100000
</code></pre></div></div> <p><br/></p> <h4 id="inputrc"><code class="language-plaintext highlighter-rouge">.inputrc</code></h4> <p>This file contains options that control the actual input at the command line. The first two lines are particularly useful since they allow you to move through previous commands more efficiently by only displaying lines in your history that begin with the text you have already typed. For example, if you start typing <code class="language-plaintext highlighter-rouge">git</code> and hit the up arrow then you will cycle through previous commands that begin with <code class="language-plaintext highlighter-rouge">git</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="s2">"</span><span class="se">\e</span><span class="s2">[A"</span>: history-search-backward
<span class="s2">"</span><span class="se">\e</span><span class="s2">[B"</span>: history-search-forward

<span class="c"># Clear screen with `control j`.</span>
<span class="s2">"</span><span class="se">\C</span><span class="s2">-j"</span>: clear-screen
</code></pre></div></div> <p><br/></p> <h4 id="vimrc"><code class="language-plaintext highlighter-rouge">.vimrc</code></h4> <p>This file sets options in <code class="language-plaintext highlighter-rouge">vim</code>, a powerful text editor that I like to use. You only need to consider these options if you use it too.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="s2">" Turn on syntax highlighting and filetype specific stuff
syntax on
filetype indent plugin on
filetype plugin on

"</span> Move backups to <span class="nb">local </span>files <span class="o">(</span>note you need to make these directories first<span class="o">)</span>
<span class="nb">set </span><span class="nv">backupdir</span><span class="o">=</span>~/.vim/vimbackup
<span class="nb">set </span><span class="nv">directory</span><span class="o">=</span>~/.vim/vimswp

<span class="s2">" Use spaces for indentation
set expandtab
set shiftwidth=4
set softtabstop=4
set autoindent

"</span> Turn on line numbers
<span class="nb">set </span>number

<span class="s2">" Display cursor's current line and column position
set ruler

"</span> Highlight search hits <span class="k">in </span>file
<span class="nb">set </span>hlsearch

<span class="s2">" Use dark background colour schemes
set background=dark

"</span> Centre with <span class="s1">'&lt;space&gt;'</span>
nmap &lt;space&gt; zz

<span class="s2">" Keep centred when searching
nmap n nzz
nmap N Nzz

"</span> Stop <span class="s1">'Ex'</span> mode being started when <span class="s1">'Q'</span> is typed
nnoremap Q &lt;nop&gt;

<span class="s2">" Stop 'q:' loading command history
map q: &lt;nop&gt;

"</span> Type aliases when trying to save and/or <span class="nb">exit
command </span>W w
<span class="nb">command </span>Wq wq
<span class="nb">command </span>WQ wq
<span class="nb">command </span>Q q

<span class="s2">" Escape current most nested parens etc
inoremap &lt;C-j&gt; &lt;Esc&gt;/[)}"</span><span class="s1">'\]&gt;]&lt;CR&gt;:nohl&lt;CR&gt;a

" Swap character under cursor with the one on its right
nnoremap &lt;silent&gt; gc xph

" Type '</span>jk<span class="s1">' or '</span>kj<span class="s1">' quickly in succession to exit insert mode
ino jk &lt;esc&gt;
ino kj &lt;esc&gt;

" Turn off highlighting with '</span>/<span class="se">\'</span>
map /<span class="se">\ </span>:noh&lt;CR&gt;

<span class="s2">" Spellchecking suggestions with ^X in normal mode
nmap &lt;C-x&gt; i&lt;C-x&gt;s

"</span> Move up/down one display line with &lt;C-k&gt;/&lt;C-j&gt; <span class="o">(</span>move within wrapped lines<span class="o">)</span>
nmap &lt;C-k&gt; gk
nmap &lt;C-j&gt; gj

<span class="s2">" Move between splits with CTRL
"</span> nmap &lt;silent&gt; &lt;C-k&gt; :wincmd k&lt;CR&gt;
<span class="s2">" nmap &lt;silent&gt; &lt;C-j&gt; :wincmd j&lt;CR&gt;
"</span> nmap &lt;silent&gt; &lt;C-h&gt; :wincmd h&lt;CR&gt;
<span class="s2">" nmap &lt;silent&gt; &lt;C-l&gt; :wincmd l&lt;CR&gt;

"</span> Enable persistent undo <span class="o">(</span>need to make undo directory first<span class="o">)</span>
<span class="nb">set </span>undofile                <span class="s2">" Save undos after file closes
set undodir=~/.vim/undo "</span> where to save undo histories
<span class="nb">set </span><span class="nv">undolevels</span><span class="o">=</span>1000         <span class="s2">" How many undos
set undoreload=10000        "</span> number of lines to save <span class="k">for </span>undo

<span class="s2">" Tell vim to remember certain things when we exit
"</span>  <span class="s1">'10  :  marks will be remembered for up to 10 previously edited files
"  "100 :  will save up to 100 lines for each register
"  :20  :  up to 20 lines of command-line history will be remembered
"  %    :  saves and restores the buffer list
"  n... :  where to save the viminfo files
set viminfo='</span>10,<span class="se">\"</span>100,:20,%,n~/.viminfo

<span class="s2">" Move to most recent position on reloading file
function! ResCur()
  if line("</span><span class="s1">'\"") &lt;= line("$")
    normal! g`"
    return 1
  endif
endfunction

augroup resCur
  autocmd!
  autocmd BufWinEnter * call ResCur()
augroup END
</span></code></pre></div></div> <h5 id="bonus-inputrc-option">Bonus <code class="language-plaintext highlighter-rouge">.inputrc</code> option</h5> <p>You can use <code class="language-plaintext highlighter-rouge">vim</code>-like editing when typing in the command line by setting these options in <code class="language-plaintext highlighter-rouge">.inputrc</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Use Vi, not Emacs, style editing</span>
<span class="nb">set </span>editing-mode vi
<span class="nb">set </span>show-mode-in-prompt on

<span class="nv">$if</span> <span class="nv">mode</span><span class="o">=</span>vi
        <span class="nb">set </span>keymap vi-insert
        <span class="c"># These are for vi-insert mode:</span>
        <span class="c"># Exit insert mode with `jk` or `kj`</span>
        <span class="s2">"jk"</span> vi-movement-mode
        <span class="s2">"kj"</span> vi-movement-mode
<span class="nv">$endif</span>
</code></pre></div></div> <p><br/></p> <h4 id="any-comments">Any comments?</h4> <p>Feel free to submit an issue <a href="https://github.com/george-hall/george-hall.github.io/issues">here</a>.</p> <p><br/></p> <hr/> <h6><small> <b>Disclaimer:</b> The content in this post is provided as is, without warranty of any kind. I make no guarantees about the completeness, reliability, or accuracy of the code, tips, or advice presented. Any action you take based on this content is strictly at your own risk. I will not be liable for any losses, damages, or issues arising from the use or misuse of this information, including (but not limited to) loss of data, system failures, or security vulnerabilities. Always test code and approaches in a safe environment before deploying them in production. </small></h6>]]></content><author><name></name></author><category term="command_line"/><category term="unix"/><category term="macos"/><category term="bash"/><category term="vim"/><summary type="html"><![CDATA[I have slowly accumulated many configuration options in my command line settings files. This post shows how I have configured bashrc, inputrc, and vimrc.]]></summary></entry><entry><title type="html">How to check whether two directories are identical</title><link href="https://george-hall.github.io/blog/2025/verifying-whether-two-directories-identical/" rel="alternate" type="text/html" title="How to check whether two directories are identical"/><published>2025-12-18T00:00:00+00:00</published><updated>2025-12-18T00:00:00+00:00</updated><id>https://george-hall.github.io/blog/2025/verifying-whether-two-directories-identical</id><content type="html" xml:base="https://george-hall.github.io/blog/2025/verifying-whether-two-directories-identical/"><![CDATA[<p>When copying a directory containing many files, it is important to make sure that everything has copied correctly. This post shows a simple way of doing so (method based on answers given <a href="https://stackoverflow.com/questions/1657232/how-can-i-calculate-an-md5-checksum-of-a-directory">here</a>).</p> <p><br/></p> <h3 id="quick-start">Quick start</h3> <p>Run the following command at the root of each directory whose integrity you want to verify (i.e. the original directory and its copy). If the outputs match, then the directories contain the same contents.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="o">{</span> find <span class="nb">.</span> <span class="nt">-type</span> f <span class="nt">-exec</span> <span class="nb">md5sum</span> <span class="o">{}</span> <span class="se">\;</span> <span class="o">&amp;&amp;</span> find <span class="nb">.</span> <span class="p">;</span> <span class="o">}</span> | <span class="nb">sort</span> <span class="nt">-k</span> 1 | <span class="nb">md5sum</span>
</code></pre></div></div> <p>Read on to see the logic behind this approach (and some common pitfalls)! We will also look at how to troubleshoot if these commands return different results.</p> <p><br/></p> <h3 id="md5-checksums"><code class="language-plaintext highlighter-rouge">md5</code> checksums</h3> <p>A <em>checksum</em> is a numeric summary of a file. There are many ways to compute them. The <code class="language-plaintext highlighter-rouge">md5</code> algorithm is a fast and simple option that can be run using the <code class="language-plaintext highlighter-rouge">md5sum</code> program. A good checksum changes substantially following even a small change to a file (e.g. a single letter being altered). For example, if we create two files, <code class="language-plaintext highlighter-rouge">file1.txt</code> and <code class="language-plaintext highlighter-rouge">file2.txt</code> that contain just “<code class="language-plaintext highlighter-rouge">text1</code>” and “<code class="language-plaintext highlighter-rouge">text2</code>”, respectively, then their <code class="language-plaintext highlighter-rouge">md5</code> checksums are completely different:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">echo</span> <span class="s2">"text1"</span> <span class="o">&gt;</span> file1.txt
<span class="nv">$ </span><span class="nb">echo</span> <span class="s2">"text2"</span> <span class="o">&gt;</span> file2.txt
<span class="nv">$ </span><span class="nb">md5sum </span>file1.txt file2.txt
a1d17e602afd232c64d1ba0015227c13  file1.txt
6994d44ab6c3b4c005357798f6b0d750  file2.txt
</code></pre></div></div> <h3 id="checksums-of-a-directory">Checksums of a directory</h3> <p><code class="language-plaintext highlighter-rouge">md5</code> checksums can only be computed for files. To compute a checksum of an entire directory, we can compute one checksum for each file in the directory, save these to a file, and then compute that file’s checksum. This will provide a single number that summarises the contents of an entire directory.</p> <p>Using this technique, we can verify whether a directory was copied correctly by computing one checksum for the original directory and one for its copy. If all files were copied correctly, then the checksums of the checksums files will match. However, if the contents of any file differs between these directories – even by a single character – then the checksum of the checksum files will differ too.</p> <p>For example, consider two directories with the following structure, where <code class="language-plaintext highlighter-rouge">file1.txt</code> contains “<code class="language-plaintext highlighter-rouge">text1</code>”, <code class="language-plaintext highlighter-rouge">file2.txt</code> contains “<code class="language-plaintext highlighter-rouge">text2</code>”, and a subdirectory <code class="language-plaintext highlighter-rouge">subdir</code> with <code class="language-plaintext highlighter-rouge">file3.txt</code>, which contains “<code class="language-plaintext highlighter-rouge">text3</code>”:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">.</span>
├── dir1
│   ├── file1.txt
│   ├── file2.txt
│   └── subdir
│       └── file3.txt
└── dir2
    ├── file1.txt
    ├── file2.txt
    └── subdir
        └── file3.txt
</code></pre></div></div> <p>We can check whether the contents of <code class="language-plaintext highlighter-rouge">dir1</code> and <code class="language-plaintext highlighter-rouge">dir2</code> are identical as follows:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># At root of dir1
$ { find . -type f -exec md5sum {} \; &amp;&amp; find . ; } | sort -k 1 &gt; ../dir1_md5sums.txt
# At root of dir2
$ { find . -type f -exec md5sum {} \; &amp;&amp; find . ; } | sort -k 1 &gt; ../dir2_md5sums.txt

# In directory containing dir1_md5sums.txt and dir2_md5sums.txt
$ md5sum dir1_md5sums.txt dir2_md5sums.txt
75a025445670330698b5f807f6308726  dir1_md5sums.txt
75a025445670330698b5f807f6308726  dir2_md5sums.txt
</code></pre></div></div> <p>The matching checksums of <code class="language-plaintext highlighter-rouge">dir1_md5sums.txt</code> and <code class="language-plaintext highlighter-rouge">dir2_md5sums.txt</code> tells us that the checksums of all files in the two directories match, meaning that the two directories are identical.</p> <p>Note that the <code class="language-plaintext highlighter-rouge">find</code> commands must be run <em>within</em> the directories (i.e. <code class="language-plaintext highlighter-rouge">find</code> for <code class="language-plaintext highlighter-rouge">dir1</code> is run at the highest level of <code class="language-plaintext highlighter-rouge">dir1</code>). This is necessary because <code class="language-plaintext highlighter-rouge">find</code> prepends the directory name to the file paths it outputs. Hence, if run outside of the directory, it would output a path containing “<code class="language-plaintext highlighter-rouge">dir1</code>” when run on <code class="language-plaintext highlighter-rouge">dir1</code> and “<code class="language-plaintext highlighter-rouge">dir2</code>” when run on <code class="language-plaintext highlighter-rouge">dir2</code>. These different paths would consequently lead to different checksums of <code class="language-plaintext highlighter-rouge">dir1_md5sums.txt</code> and <code class="language-plaintext highlighter-rouge">dir2_md5sums.txt</code>.</p> <p>In some cases, it might be important to ensure that empty directories have been copied into the correct locations. Since these directories would be missed by the initial <code class="language-plaintext highlighter-rouge">find</code> command (which only looks for files), we also run <code class="language-plaintext highlighter-rouge">find .</code> to return the overall structure of the directory (this time including empty directories). We chain together the commands within braces to allow us to sort the outputs using a single command. We sort the outputs in case files are listed in a different order when running in different directories, which would lead to different directory checksums even though the contents of each file and the directory structure itself may have been preserved.</p> <p>Putting this all together, the above command creates a checksum file for <code class="language-plaintext highlighter-rouge">dir1</code> that looks like:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cat </span>dir1_md5sums.txt
<span class="nb">.</span>
./file1.txt
./file2.txt
./subdir
./subdir/file3.txt
4ca8392d7fe0ee3dfe17ec2193ee79d0  ./subdir/file3.txt
6994d44ab6c3b4c005357798f6b0d750  ./file2.txt
a1d17e602afd232c64d1ba0015227c13  ./file1.txt
</code></pre></div></div> <p>Let’s see what happens if we change the text in <code class="language-plaintext highlighter-rouge">file1.txt</code> in <code class="language-plaintext highlighter-rouge">dir1</code> to <code class="language-plaintext highlighter-rouge">text4</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s2">"text4"</span> <span class="o">&gt;</span> dir1/file1.txt
<span class="c"># At root of dir1</span>
<span class="nv">$ </span><span class="o">{</span> find <span class="nb">.</span> <span class="nt">-type</span> f <span class="nt">-exec</span> <span class="nb">md5sum</span> <span class="o">{}</span> <span class="se">\;</span> <span class="o">&amp;&amp;</span> find <span class="nb">.</span> <span class="p">;</span> <span class="o">}</span> | <span class="nb">sort</span> <span class="nt">-k</span> 1 <span class="o">&gt;</span> ../dir1_md5sums.txt
<span class="c"># At root of dir2</span>
<span class="nv">$ </span><span class="o">{</span> find <span class="nb">.</span> <span class="nt">-type</span> f <span class="nt">-exec</span> <span class="nb">md5sum</span> <span class="o">{}</span> <span class="se">\;</span> <span class="o">&amp;&amp;</span> find <span class="nb">.</span> <span class="p">;</span> <span class="o">}</span> | <span class="nb">sort</span> <span class="nt">-k</span> 1 <span class="o">&gt;</span> ../dir2_md5sums.txt

<span class="c"># In directory containing dir1_md5sums.txt and dir2_md5sums.txt</span>
<span class="nv">$ </span><span class="nb">md5sum </span>dir1_md5sums.txt dir2_md5sums.txt
83d3e2336d67208a9598aa414f80be3c  dir1_md5sums.txt
857d78aeec4dff651d371c2ece4782d6  dir2_md5sums.txt
</code></pre></div></div> <p>Now the two checksums are different! This tells us that there is a difference somewhere in the directories. If <code class="language-plaintext highlighter-rouge">dir1</code> were the original directory and <code class="language-plaintext highlighter-rouge">dir2</code> a copy of it, we could conclude that the copy has been unsuccessful as the contents of at least one file differs between the two directories.</p> <p><br/></p> <h3 id="faqs">FAQs</h3> <h5 id="how-do-i-know-which-files-differ">How do I know which files differ?</h5> <p>In our simple case above, we can identify the file that differs by comparing the checksums contained in <code class="language-plaintext highlighter-rouge">dir1_md5sums.txt</code> and <code class="language-plaintext highlighter-rouge">dir2_md5sums.txt</code> by eye. If we have many files, however, this isn’t feasible. In that case, we could compare the two checksums files using <code class="language-plaintext highlighter-rouge">comm</code>, which will print the lines that differ. <code class="language-plaintext highlighter-rouge">comm -23</code> prints lines in the first file that aren’t in the second.</p> <p>Recall that, in the above example, the directories differ only in the contents of <code class="language-plaintext highlighter-rouge">file1</code>. Thus <code class="language-plaintext highlighter-rouge">comm -23</code> outputs only the checksum for that file:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">comm</span> <span class="nt">-23</span> dir1_md5sums.txt dir2_md5sums.txt
9ac5e4b278717acc79bd4cbf3b79c6b6  ./file1.txt
</code></pre></div></div> <p><br/></p> <h5 id="can-two-different-files-generate-the-same-checksum">Can two different files generate the same checksum?</h5> <p>Yes, but the chances are vanishingly small that this happens at random. An <code class="language-plaintext highlighter-rouge">md5</code> checksum is 32 digits long and each digit takes one of 16 values (i.e. it is hexadecimal), which means that there are 16^32 = 340,282,366,920,938,463,463,374,607,431,768,211,456 possibilities. Given that <code class="language-plaintext highlighter-rouge">md5</code> is designed to distribute checksums across all these options, you can see that it is exceedingly unlikely for you to generate two files which differ in content but have the same checksum.</p> <p>In theory (and in practice!), an attacker could construct two files that have the same <code class="language-plaintext highlighter-rouge">md5</code> checksum (<code class="language-plaintext highlighter-rouge">md5</code> has been considered broken for security purposes for decades). But as long as you are not in a scenario where someone might be maliciously non-identical files with identical checksums to mess with your copying, you can safely assume that no two files with different contents will share a checksum.</p> <p><br/></p> <h5 id="why-does-an-identical-directory-has-different-checksums-on-different-computers">Why does an identical directory has different checksums on different computers?</h5> <p>I have seen this happen in cases where the <code class="language-plaintext highlighter-rouge">sort</code> command differs slightly between operating systems (e.g. maybe numbers are considered ‘smaller’ than letters on MacOS but ‘larger’ than letters on Linux). This leads to different orders of files in the checksum file. A simple workaround is to copy the two checksum files to the same machine, sort them there and compare the checksums of the re-sorted files. If these match, then the directories are identical.</p> <p><br/></p> <h5 id="how-can-i-get-in-touch-about-this-post">How can I get in touch about this post?</h5> <p>Feel free to submit an issue <a href="https://github.com/george-hall/george-hall.github.io/issues">here</a>.</p> <p><br/></p> <hr/> <h6><small> <b>Disclaimer:</b> The content in this post is provided as is, without warranty of any kind. I make no guarantees about the completeness, reliability, or accuracy of the code, tips, or advice presented. Any action you take based on this content is strictly at your own risk. I will not be liable for any losses, damages, or issues arising from the use or misuse of this information, including (but not limited to) loss of data, system failures, or security vulnerabilities. Always test code and approaches in a safe environment before deploying them in production. </small></h6>]]></content><author><name></name></author><category term="command_line"/><category term="unix"/><category term="macos"/><category term="bash"/><summary type="html"><![CDATA[If you are transferring your files, you need to make sure that they have copied correctly! This post shows a simple way to do so.]]></summary></entry></feed>